type User {
    id: ID!
    name: String!
    email: String!
}

input UserInput {
    name: String!
    email: String!
}

type Query {
    getUsers: [User!]!
}

type Mutation {
    upsertUser(input: UserInput!): User!
}